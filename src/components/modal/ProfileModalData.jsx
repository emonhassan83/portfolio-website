const UpdateProjectModalData = ({project}) => {
    console.log(project);

    return (
        <div>
            this is project modal data
        </div>
    );
};

export default UpdateProjectModalData;